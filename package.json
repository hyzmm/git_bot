{
  "type": "module",
  "dependencies": {
    "axios": "^0.24.0",
    "express": "^4.17.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.8",
    "prettier": "^2.5.1",
    "typescript": "^4.5.4"
  },
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  }
}
